let massiv = [ 'apple', 'banana', 'orange']

// push - добавляет в конец массива
massiv.push('grape')

// unshift - добавяет к начало массива
massiv.unshift('cherry')

// pop - удаляет последний элемент в массиве
massiv.pop()

// shift - удаляет первый элемент в массиве
massiv.shift()


let fruits = ['apple', 'banana', 'orange']

let fruits2 = ['mango', 'melon']

// concat объединяет массивы
let all = fruits.concat(fruits2)

console.log(all);


let fruitss = ['apple', 'banana', 'orange']

// преобразует массив в единную строку
let all1 = fruits.join(', ')

console.log(all);
